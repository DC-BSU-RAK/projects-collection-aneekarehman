<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.ApolloLitApp1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Suppress the default Android 12+ splash icon to "remove the robot" splash -->
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="31">@android:color/transparent</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="31">@android:color/black</item>
    </style>

    <style name="PopupActivityTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>